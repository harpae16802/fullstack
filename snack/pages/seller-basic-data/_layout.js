// pages/seller-basic-data/_layout.js
import Layout from '../../components/Layout'; // 根据实际路径引入 Layout 组件

export default function SellerBasicDataLayout({ children }) {
  return <Layout>{children}</Layout>;
}
